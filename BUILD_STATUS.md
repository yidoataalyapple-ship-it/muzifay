# Build Status

Last build trigger: Workflow updated with iOS support, timeouts, and npm install step.

## Changes Applied
- `.gitattributes` added to protect binary files
- `credentialsSource: local` removed from eas.json (using Expo managed credentials)
- GitHub Actions workflow enhanced with Android + iOS builds, timeout, and concurrency control
